
const initialState = {
    isLogged: false
}

export default (state = initialState, action) => {
    switch(action.type)
    {
        
    }

    console.log("Runninng login reducer, state: ", state)

    return state;
}
